# Normalise line endings to LF in the repository.
# Without this, core.autocrlf=true on Windows checks files out as CRLF and every
# diff on another machine shows whole-file changes.
* text=auto eol=lf

# Binary — never touch these.
*.png binary
*.jpg binary
*.gif binary
*.svg text eol=lf
*.zip binary
